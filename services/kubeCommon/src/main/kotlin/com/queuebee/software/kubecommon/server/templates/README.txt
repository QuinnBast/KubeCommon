Application will store templates for various Kubernetes fields and types so that applying/updating a manifest or helm chart is easy.

The templates will "auto-detect" common fields and types and the UI will provide a way to easily configure these templates in-place.